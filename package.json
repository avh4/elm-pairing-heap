{
  "scripts": {
    "test": "elm-test && elm make --docs=documentation.json && elm-format --validate .",
    "start": "chokidar --initial elm.json 'src/**/*.elm' 'tests/**/*.elm' -c 'npm test'"
  },
  "devDependencies": {
    "chokidar-cli": "^1.2.2",
    "elm": "^0.19.0-no-deps",
    "elm-format": "^0.8.1",
    "elm-test": "^0.19.0-rev6"
  }
}
