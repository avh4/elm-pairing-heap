{
  "scripts": {
    "test": "elm-test && elm make --docs=documentation.json && elm-format --validate ."
  },
  "devDependencies": {
    "elm": "^0.19.0-no-deps",
    "elm-format": "^0.8.1",
    "elm-test": "^0.19.0-rev6"
  }
}
